# FermiHDI Border0 Coder Glue Logic - Configuration

# Border0 Administration Token
# Supports: Literal value OR absolute file path (e.g. /etc/secrets/border0_token)
BORDER0_ADMIN_TOKEN=your_token_or_path

# The ID of the Border0 Connector
# Supports: Literal value OR absolute file path
BORDER0_CONNECTOR_ID=your_id_or_path

# Global Policy ID to attach to every socket (e.g. for management access)
# Replace MGMT_EMAILS with this existing policy ID from your Border0 portal
BORDER0_GLOBAL_POLICY_ID=your_predefined_policy_id

# Deployment Mode: 'docker' or 'k8s'
DEPLOYMENT_MODE=docker

# Port for the Express app
PORT=8000
